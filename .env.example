# 博客园同步（assemble-publish）示例环境变量（最小可用）
# 复制为 .env 并按需填写；或在部署平台里配置同名环境变量

# 博客园（MetaWeblog）认证
CNBLOGS_RPC_URL=
CNBLOGS_USERNAME=
CNBLOGS_TOKEN=

# 主仓库
SYNC_REPO_URL=
SYNC_REPO_TOKEN=

# Optional: run dedup after each sync (tools/deduplicate_cnblogs.py)
POST_DEDUP=false

# Dedup options
DEDUP_DRY_RUN=false
DEDUP_KEEP_LATEST=true
DEDUP_SHOW_DETAILS=false
DEDUP_DELETE_DELAY=0
DEDUP_MAX_ROUNDS=50
